# 碩士論文題目發想（基於 TuRTLe 專案）

日期：2026-08-10

## 前言

這份文件是根據目前專案已經累積的東西整理出來的候選題目，不是憑空發想。目前手上已經有的基礎設施跟證據：

- **TuRTLe 評測框架**：涵蓋 5 個 RTL 生成 benchmark（RTLLM、VerilogEval、VGen、RTL-Repo、NotSoTiny），指標涵蓋語法（STX）、功能正確性（FNC）、可合成性/等價性（SYN/EQV）、PPA（面積/功耗/效能）——這是少數同時做到「功能 + PPA」雙軌評測的框架，比單純看 pass@1 的評測完整。
- **本地推論管線**：從 HuggingFace 原始權重手動轉 GGUF、量化（F16/Q6_K/Q5_K_M）、接 Ollama，整套在單張 8GB VRAM 筆電 GPU 上跑完，且**已經實測到量化不足時的具體故障模式**（F16 版本 VRAM 溢出到共用記憶體，GPU 使用率掉到個位數、單題耗時破百秒）。
- **LoRA/QLoRA 微調管線**：對 `Qwen2.5-Coder-7B-Instruct` 做 4-bit QLoRA SFT，訓練語料是從 5 個公開 Verilog 資料集（RTL-Coder-27k、CodeV-All、VeriGen-GitHub-Corpus 等，合計約 30 萬筆原始樣本）用 iverilog 做語法驗證、去重後產出的 20 萬筆語料。
- **一個已經跑出來、還沒解釋清楚的反直覺結果**（`LoRA_train/COMPARISON_RESULTS.md`）：LoRA 微調在 RTLLM 上讓模型六項指標**全部變差**，但資料量從 2% 拉到 10% 後，六項指標同步往 baseline 方向回升（差距縮小但沒追上）。這個「資料量越大、負遷移越小」的趨勢本身就是一個值得深入挖的現象。

下面每個題目都會標明：研究問題、跟現有東西的關聯（能直接沿用多少）、具體實驗設計、跟既有文獻的差異點、風險。

---

## 題目 A：LoRA 微調對 RTL 生成造成「負遷移」的成因診斷與緩解

### 研究問題
為什麼對通用 code LLM 做 RTL 領域的 LoRA 微調，反而讓下游 benchmark 表現變差？負遷移的主因是資料量不足、訓練語料的介面/命名慣例跟評測集不合、還是訓練語料本身品質不夠（只驗證語法沒驗證功能）？

### 跟現有基礎的關聯
`COMPARISON_RESULTS.md` 已經提出三個候選成因（資料量、命名慣例落差、語料只做語法驗證未做功能驗證），但目前只有 2% / 10% 兩個資料量的點，且三個成因彼此**沒有拆開做控制變因實驗**。這題本質上是把現有的「意外發現」systematic 化。

### 實驗設計
1. **資料量 scaling curve**：2% / 10% / 20% / 50% / 100%，固定其他超參數（r=16, alpha=32），畫出六項 RTLLM 指標 vs. 資料量的曲線，確認是單調回升還是在某個比例後就飽和/反轉。
2. **拆解命名慣例假說（獨立變因）**：構造一個「把訓練語料的 instruction/module 命名風格改寫成跟 RTLLM 一致」的子集（例如用規則式改寫或再用 LLM 做 paraphrase），在**同樣資料量**下比較改寫前後的 syntax pass@1，隔離出命名慣例造成的影響有多大。
3. **拆解語料品質假說**：跟題目 B 共用同一份「功能驗證過」的子集，比較「同資料量、有無功能驗證過濾」對六項指標的影響。

### 貢獻
目前 RTL-LLM 微調的論文（RTLCoder、CodeV 等）大多只報「微調後比 baseline 好」的正面結果，很少有人系統性地分析「什麼情況下微調會比不微調更差」——這是一個負面結果但有清楚因果拆解的貢獻，對後續想做 RTL 領域微調的人有直接的實務參考價值（避開哪些坑）。

### 風險
三個變因之間可能有交互作用（例如資料量夠大時，命名慣例的影響會被稀釋），純粹的控制變因實驗可能要跑到 3×5=15 組以上的訓練，在 8GB VRAM + QLoRA 的速度下（10% 資料量已經要 14 小時），時間成本是最大的風險，需要精簡實驗矩陣或申請額外算力。

---

## 題目 B：功能正確性感知的 RTL 訓練資料過濾（Functional-Correctness-Aware Data Curation）

### 研究問題
目前公開的 RTL LLM 訓練語料（RTL-Coder、CodeV 等）都只保證/驗證「能編譯過」，沒有任何一個保證「邏輯是對的」。如果用某種方式過濾出「功能上更可能正確」的子集來微調，能不能讓微調結果轉為正貢獻（甚至超越 baseline）？

### 跟現有基礎的關聯
`VALIDATION_REPORT.md` 已經明確點出這個限制（只做了 `iverilog` 編譯檢查，沒做 `vvp` 執行、更沒有 testbench 驗證邏輯），並且列為「已知限制」的第一條。這題直接是那份報告「後續待辦」的延伸，而且 iverilog/vvp 的驗證管線已經寫好、跑過一輪，可以直接加一層而不是從零開始。

### 實驗設計
1. **Self-consistency 過濾**：對每筆訓練樣本的 instruction，讓一個強模型（或多次取樣同一模型）重新生成程式碼，跟原始樣本做「輸出是否一致」或「用 LLM 生成的簡易 testbench 跑 `vvp` 是否行為一致」的比對，一致度高的樣本視為「品質較高」。
2. **形式等價/性質檢查過濾**（進階版）：對能配對出簡單性質（如組合邏輯電路）的樣本，用 Yosys 等價檢查或簡單的 property assertion 做二階過濾，這部分可以直接沿用 TuRTLe 本身已經在用的 Yosys/EQV 管線。
3. **對照實驗**：同資料量下，「未過濾」vs「過濾後」子集微調，比較 RTLLM 六項指標，並額外看**過濾後資料量減少造成的損失**跟**品質提升帶來的收益**兩者的權衡點在哪裡。

### 貢獻
把「資料品質可以用自動化方式代理驗證（proxy verification）」這個在通用程式碼 LLM 訓練已經有人做（如 self-consistency filtering、unit-test filtering）的做法，系統性地搬到 RTL 這個領域，並且用真實下游 benchmark（不是訓練 loss）驗證效果——這是題目本身的 novelty：多數 RTL 資料集論文只做到「語法驗證」就停了。

### 風險
RTL 缺乏自動化 testbench 生成的成熟工具（不像 Python 有豐富的 test framework），self-consistency/性質檢查的覆蓋率可能有限，需要先做一輪小規模可行性驗證（挑幾百筆樣本看能不能自動化跑出有意義的訊號）再決定要不要投入全量處理。

---

## 題目 C：閉環自我修正微調（Error-Driven Iterative LoRA）

### 研究問題
如果讓模型「在 benchmark 上生成失敗的題目」直接變成下一輪訓練資料的來源（收集編譯錯誤/功能錯誤的 pattern，針對性補強），能不能比一次性用大量通用語料微調更有效率？

### 跟現有基礎的關聯
`TRAINING_GUIDE.md` 的「已知風險」第一條就寫了「只做了單輪 SFT，沒有實作之前討論過的『自我修正 + 迭代 LoRA』那整套閉環」——這題就是把那個當時只有討論、沒有實作的架構做出來。現有的生成 pipeline（`run_turtle.sh`）、錯誤分類（TuRTLe 的 report.json 已經有 `error` 欄位，例如 `Runtime error`、`Unable to Bind Wire/Reg`、`Async reset found` 等分類）都可以直接重用來當作「收集失敗案例」的資料來源。

### 實驗設計
一個典型的迭代迴圈：
```
第 0 輪：baseline 模型在 held-out 訓練/驗證集（非 TuRTLe benchmark，避免資料洩漏）上生成
      → 分類失敗案例（依 error type）
第 1 輪：針對失敗最多的 error type，構造「錯誤示範 + 正確修正」的訓練樣本
      → LoRA 微調
      → 重新生成、重新分類失敗案例
第 2 輪：重複，直到失敗率不再下降或資源用盡
```
比較「固定一次性用全量資料訓練」vs「迭代 N 輪、每輪資料量相同但由錯誤導向挑選」的最終 benchmark 表現與訓練樣本效率（達到同樣分數所需的樣本數）。

### 貢獻
這個做法在通用程式碼生成領域有類似精神的工作（如 Reflexion、STaR、self-refine），但應用在 RTL 這種「錯誤型態高度結構化」（TuRTLe 本身就已經把錯誤分成 syntax/runtime/async-reset 等明確類別）的領域，錯誤導向的資料構造可以做得比通用領域更精準，這是題目的差異化空間。

### 風險
是三個題目裡工程複雜度最高的（需要自動化的錯誤分類 → 資料構造 → 重新訓練整條 pipeline），加上每輪都要重新跑 QLoRA 訓練（10% 資料量就要 14 小時），迭代次數會被時間預算限制在 2-3 輪，需要提早規劃時間軸，比較適合當作碩論的核心但要有明確的 scope-down 計畫（例如先只針對 1-2 種最常見的 error type 做閉環驗證，而不是全部 error type）。

---

## 題目 D：量化程度對「結構敏感」任務（RTL 語法）的影響是否比通用程式碼任務更嚴重

### 研究問題
量化對 LLM 輸出品質的影響在通用程式碼生成（Python/JS 等）上已經有不少研究，但 RTL/Verilog 有更嚴格的語法結構要求（module/port 命名必須精確匹配 testbench、時序語意敏感）。量化程度（F16/Q6_K/Q5_K_M/Q4_K_M）對 RTL 生成的 syntax pass@1 造成的下降幅度，是否顯著大於對功能正確性（func pass@1）或對通用程式碼任務的影響？

### 跟現有基礎的關聯
專案裡已經有同一顆模型（RTLCoder-Deepseek-v1.1）的三種量化版本（F16/Q6_K/Q5_K_M）並列在 Ollama 裡，且已經實測過 F16 在 8GB VRAM 上會因為 KV cache 過大（這個模型是 full multi-head attention、沒有 GQA，KV cache 開銷天生比現代模型大）而效能崩潰——這個實測現象本身就可以寫成論文裡「為什麼消費級硬體上量化是必要而非可選」的動機段落。

### 實驗設計
用現成的三個量化版本（可以再補 Q4_K_M 讓梯度更完整）跑滿 TuRTLe 五個 benchmark，比較：
1. syntax pass@1 隨量化程度下降的幅度
2. func pass@1（僅在通過語法的子集上）隨量化程度下降的幅度
3. 兩者下降幅度的比值，驗證「語法結構」是否比「邏輯正確性」對量化更敏感
4. 額外可以把 PPA 指標也納入，看量化是否連帶影響生成程式碼的合成品質（面積/功耗/效能）

### 貢獻
現有的 LLM 量化研究幾乎都集中在通用語言/程式碼任務，RTL 這種「格式必須精確匹配」的下游任務對量化的敏感度目前沒有系統性研究，這題可以填這塊空白，且**完全不需要額外訓練**（只需要跑推論 + 評測），是六個題目裡工程風險最低、最容易在時間內做完的。

### 風險
新穎性相對題目 A/B/C 較低（比較像是「把已知的量化評測方法論套用到新領域」），如果要提高貢獻度，建議至少搭配題目 A 或 B 的部分內容，或加入「為什麼是這樣」的機制分析（例如量化誤差是否集中在特定 token 位置，如 module 宣告或 port 名稱附近）。

---

## 題目 E：通用型 vs. RTL 專用型 Code LLM 的系統性比較

### 研究問題
在相近參數規模（7B 級）下，RTL 領域專用模型（如 RTLCoder-Deepseek-v1.1，論文訓練時用 GPT-3.5 生成語料）相較於通用型 code LLM（如 Qwen2.5-Coder-7B），在 TuRTLe 的五個 benchmark 上是否真的全面勝出？如果不是全面勝出，勝負分佈在哪些任務類型/指標上？

### 跟現有基礎的關聯
兩顆模型都已經在專案裡跑起來了，只差把完整的五個 benchmark、六項指標都補齊、做成系統性的比較表格，工程量最小。

### 貢獻
比較偏向 survey/benchmark 型的貢獻，novelty 較低，但可以作為題目 A-D 任一個的「背景章節」或給業界/其他研究者一個實用的選模型參考，比較適合當輔助章節而非獨立碩論主軸。

---

## 建議

單獨挑一個當碩論主軸的話，個人建議順序：

1. **題目 B（功能正確性感知的資料過濾）** — novelty 夠、直接接在你已經做完的語法驗證管線之後，而且「訓練資料只驗證語法沒驗證功能」是你自己在 `VALIDATION_REPORT.md` 裡已經點名的限制，做完等於直接回應自己提出的問題，敘事完整。
2. **題目 A（負遷移成因診斷）** — 如果想要一個「解釋現象」而非「提出新方法」導向的碩論，這個最貼合你手上已經有的反直覺實驗結果，而且控制變因實驗的設計相對單純。
3. **題目 C（閉環自我修正）** — 天花板最高、最有機會投到好一點的場合，但工程複雜度跟時間風險也最高，建議只有在確定能拿到足夠算力（例如能用學校的 GPU cluster 而不是全靠這台筆電）時才選。

題目 D、E novelty 較低，適合當主論文裡的一個子章節（例如 D 可以放進 A 的「動機」或「附加分析」，E 可以放進任一題的相關工作/背景比較），不建議單獨當主軸。

也可以把 A + B 合併成一篇（「診斷負遷移成因，並針對其中資料品質這個成因提出過濾方法」），敘事上會比兩篇獨立的更完整，是四個題目裡我覺得最平衡（novelty、可行性、跟現有工作的銜接度）的組合。
